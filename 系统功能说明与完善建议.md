# 万州区生态环境局智能 AI 大数据控制台 — 系统功能说明与完善建议

> 文档生成日期：2026-06-08
> 适用版本：当前主干代码（前端 React + Vite，后端 Node.js + JSON 文件存储）

---

## 一、系统概述

本系统是面向生态环境监管的**多协议视频监控与大气预警驾驶舱**，整合视频监控、大气质量采集与预警、水质监测、设备状态、AI 告警、地图态势等能力，提供统一的可视化大屏与后台管理。

系统采用前后端分离架构：

- **前端**：React 单页应用，负责数据可视化、视频播放、后台管理交互
- **后端**：Node.js + Express 服务，负责视频流配置、地图点位、设备接入、流转发、大气数据采集、预警判断
- **数据一致性**：后端为唯一数据源，前端通过轮询自动同步，内外网多客户端数据一致

---

## 二、技术栈

### 前端

| 类别 | 技术 |
|------|------|
| 框架 | React 18.3 + TypeScript |
| 构建 | Vite 6.3 |
| 样式 | TailwindCSS 4.1 + 内联样式 |
| UI 组件 | Radix UI、Material-UI 图标、Lucide |
| 地图 | 高德地图 API 2.0 |
| 视频播放 | mpegts.js（HTTP-FLV）、原生 video（HLS/MP4）、RTCPeerConnection（WebRTC） |
| 图表 | Recharts + 自定义 SVG |

### 后端

| 类别 | 技术 |
|------|------|
| 运行环境 | Node.js 18+ |
| HTTP 框架 | Express 4.18 |
| 数据存储 | JSON 文件（纯 JS，无原生依赖，无需编译） |
| HTML 解析 | cheerio（网页爬虫） |
| 媒体转发 | ffmpeg（子进程调用） |
| 设备发现 | node-onvif（可选） |

> 说明：早期曾尝试 SQLite（better-sqlite3），因 Windows + Node 22 缺编译工具链无法安装，已改为纯 JSON 文件存储，零编译依赖。

---

## 三、功能模块说明

### 3.1 大屏驾驶舱

**顶部栏（TopBar）**：系统标题、实时时间、天气、AQI、系统状态、管理后台入口。

**左侧面板（LeftPanel）**：
- 大气环境质量：周家坝 / 百安坝双站切换，展示 AQI 及 PM2.5、PM10、SO₂、NO₂、O₃、CO 六项指标和迷你趋势图。**数据来自后端真实采集**（airQualityData），最新一条记录渲染指标，近 14 条历史绘制趋势线，无数据时降级静态兜底
- 水质监测数据、设备在线状态：固定比例展示，杜绝面板漂移
- 「更多」弹窗（AirQualityModal）：展示近 24 小时整点表格与图表，打开时自动从后端同步，每 5 分钟刷新，过滤 AQI=0 的无效数据

**中央地图（CenterPanel + MapView）**：基于高德地图，三视图切换（全域态势 / 气环境 / 水环境）。所有点位（大气站、水质站、污染源摄像头、告警点、无人机机场、流域监测）**全部从后端 mapPoints / videoStreams 动态读取**，不再硬编码。

**右侧面板（RightPanel）**：告警面板、视频轮巡（点击打开播放器）、统计分析。

**视频播放器（VideoPlayerModal）**：支持 HTTP-FLV、WebRTC、HLS、RTSP（经转发）；1/4/9 宫格布局切换；断流自动重连（最多 5 次，指数退避 1/2/4/8/16 秒）。

### 3.2 管理后台

| 页面 | 功能 |
|------|------|
| 系统总览 | 连接状态、数据统计、实时数据日志 |
| 视频流管理 | 摄像头增删改查，支持 RTSP/HLS/WebRTC/ONVIF/GB28181，含国标参数表单；上下线、改名、改分组、改地址实时持久化 |
| MQTT 配置 | Broker 连接与 Topic 订阅管理 |
| 告警接入 | 告警 JSON 字段映射、等级/类型转换、推送测试 |
| 市局监测站数据 | 大气数据查看/录入，**支持从采集模块一键同步并每 10 分钟自动同步** |
| 气体采集预警 | 数据源配置、采集触发、预警记录、预警规则、采集日志（详见 3.3） |

### 3.3 气体采集预警模块

完整的"采集 → 校验 → 入库 → 预警判断 → 展示"链路。

**数据采集**：
- 支持 `cq_api`（JSON 接口）和 `html_crawl`（网页爬取）两种实采类型，及 http/mqtt/mysql/tcp 占位类型
- 已对接重庆市空气质量发布系统 `getThirtySixHourAQI` 接口（POST，body 形如 `stationname=周家坝`），返回 36 小时数据
- 字段映射：pm25→PM2.5、pm10→PM10、so2→SO₂、no2→NO₂、o3→O₃、co→CO、regioncode→区县名
- **定时调度器**：每 15 秒检查，按各数据源 `collect_cycle`（默认 300 秒）独立自动采集；最低周期 30 秒
- 异常处理：超时重试、连续失败 5 次熔断 10 分钟（可手动解除）、网页结构诊断接口

**数据校验**：非空、时间格式、时间合理性（拒未来/超24h）、AQI 0-500、点位+时间去重。36 小时数据按时间升序入库，保证预警历史窗口正确。

**预警规则（6 种污染物，4 种类型）**：

| 污染物 | 不预警 | 5小时增长预警 | 跨阈值预警 | 固定值 |
|--------|--------|---------------|------------|--------|
| PM2.5 | ≤30 | 30<v≤60 且5h增长≥40% | 跨 75/115/150 | — |
| PM10 | ≤45 | 45<v≤120 且5h增长≥40% | 跨 150/250/350 | — |
| SO₂ | <20 | — | — | — |
| NO₂ | ≤30 | >30 且5h增长≥40% | — | — |
| O₃ | ≤160 | — | 跨 160 | — |
| CO | ≤1 | — | — | >1 |

优先级：跨阈值 > 固定值 > 5小时增长 > 不预警。5小时增长以"当前+前4小时"窗口最低值为基准；跨阈值要求前一小时在阈值下、当前跨到阈值上。

---

## 四、后端 API 接口清单

**视频流**：GET/POST `/api/streams`，PATCH/DELETE `/api/streams/:id`

**地图点位**：GET/POST `/api/map-points`（GET 支持 `?type=`），PATCH/DELETE `/api/map-points/:id`

**流转发**：POST `/api/stream/start`，DELETE `/api/stream/stop/:id`

**ONVIF**：POST `/api/onvif/scan`，GET `/api/onvif/devices`

**GB28181**：POST `/api/gb28181/invite`

**数据源**：GET/POST `/api/datasources`，PATCH/DELETE `/api/datasources/:id`，POST `/api/datasources/:id/test`、`/clear-breaker`、`/diagnose`

**采集**：POST `/api/collect/run/:id`，GET `/api/collected`、`/api/collected/as-aq`

**预警**：GET `/api/warnings`、`/api/warning-rules`

**日志**：GET `/api/collect-logs`

**健康检查**：GET `/api/health`、`/api/health/files`，POST `/api/health/restore`

---

## 五、数据存储与可靠性

**存储**：纯 JSON 文件，位于 `server/data/` 目录（与源码隔离）：
streams.json、map_points.json、datasources.json、collected.json（≤5000条）、warnings.json（≤2000条）、collect_logs.json（≤500条）。

**可靠性机制**（经多次故障迭代加固）：

1. **数据与源码隔离**：数据文件放 `data/` 子目录，`dev` 脚本用 `--watch-path` 只监视 js 文件——根治了此前 `node --watch` 监视到数据写入而重启进程、导致文件被截断损坏的问题
2. **写入自校验**：序列化后先自解析，再写唯一临时文件（含进程号+时间戳防并发），回读校验通过才 rename——杜绝写出半截损坏文件
3. **滚动备份**：每次写入前备份上一版，保留最近 3 份（.bak1/.bak2/.bak3），仅备份合法非空数据
4. **启动自愈**：启动时检测数据文件，为空或损坏则自动用默认种子数据重建
5. **健康检查与恢复**：`/api/health/files` 查看每个文件状态，`/api/health/restore` 一键从备份恢复

**数据流（视频流为例）**：
```
后端 JSON 文件（唯一数据源）
       ↑↓  CRUD API
前端内存状态（每 10 秒轮询同步）
       ↓
所有客户端（内外网一致）
```

---

## 六、协议接入能力现状

| 协议 | 状态 | 说明 |
|------|------|------|
| HTTP-FLV | ✅ 可用 | mpegts.js 直接播放 |
| HLS | ✅ 可用 | 原生 video 播放 |
| WebRTC/WHEP | ⚠️ 基础可用 | 已实现 SDP 协商，仅 STUN，缺 TURN |
| RTSP | ✅ 可用（需 ZLMediaKit） | 经 ZLM addStreamProxy 转 FLV/WebRTC，前端自动请求转换 |
| ONVIF | ⚠️ 仅发现 | 可发现设备，未实现取流 |
| GB28181 | ⚠️ 半可用 | 已对接 ZLM 取流地址，设备注册推流仍需 WVP-PRO 信令 |

---

## 七、完善建议

### 7.1 高优先级

1. ~~**接入流媒体服务器**~~（✅ 已完成 Sprint 4）：新增 server/zlm.js 封装 ZLMediaKit REST API；/api/stream/start 优先用 ZLM 拉流代理（addStreamProxy）输出 FLV/WebRTC，ZLM 不可达时降级 ffmpeg；前端 RTSP/ONVIF/GB28181 流自动经后端转换再播放；新增 ZLM 配置接口与《ZLMediaKit部署指南.md》。
2. **补齐 GB28181 接入**：现为占位，建议引入 WVP-PRO 处理 SIP 信令与媒体协商。
3. **后端接口鉴权**：所有接口当前零鉴权，外网暴露场景建议加 JWT 或 API Key。
4. ~~**采集数据质量治理**~~（✅ 已完成 Sprint 2）：后端入库时对 AQI≤0 且无有效污染物的记录打 `valid:false` 标记，留痕但不参与预警与历史窗口。

### 7.2 中优先级

5. **统一错误提示**：前端部分 fetch 静默吞错，建议接入全局提示（已引入 sonner）。
6. ~~**设备在线状态动态检测**~~（✅ 已完成 Sprint 3）：新增 server/stream-monitor.js，每 60 秒探测启用视频流地址可达性（HTTP/FLV/HLS 用 fetch，RTSP 用 ffprobe），结果写入 autoOffline+lastCheckedAt，与手动 offline 分离；新增 GET /api/streams/health，前端展示探测状态与最后检测时间。
7. **WebRTC 增加 TURN**：仅 STUN 在跨 NAT 场景易失败。
8. **短信推送落地**：气体采集预警模块的短信联系人、模版、云 MAS 接口、推送历史目前为占位（开发文档已规划），下一阶段实现。

### 7.3 低优先级

9. **配置外部化**：高德地图 Key 硬编码在前端，建议迁移到环境变量。
10. **MQTT 真实接入**：当前为模拟连接，建议接入真实 Broker。
11. ~~**日志系统**~~（✅ 已完成 Sprint 2）：后端引入零依赖结构化日志（server/logger.js），分级 + 时间戳 + 模块名，LOG_FILE=1 可写文件。
12. **采集数据可视化增强**：基于已入库的历史数据做趋势分析、超标统计报表。

---

## 八、近期已完成的关键变更

- 视频流/地图点位数据后端化，前端轮询同步，解决内外网数据不一致
- 存储从 SQLite 改为 JSON 文件（解决编译依赖问题）
- 新增气体采集预警完整模块（数据源配置、网页/接口采集、定时调度、6 污染物预警引擎、采集日志）
- 对接重庆市空气质量 getThirtySixHourAQI 接口
- 前台大气环境质量面板（LeftPanel）及「更多」弹窗对接后端真实采集数据，自动同步
- 市局监测站数据页支持从采集模块一键同步 + 10 分钟自动同步
- 数据可靠性大幅加固：data/ 目录隔离、写入自校验、滚动备份、启动自愈、健康检查与恢复接口
- **Sprint 1**：API Key 接口鉴权（写操作强制校验，GET 放行）、前端 apiFetch 统一请求与错误处理、管理后台 Token 设置入口
- **Sprint 2**：零依赖结构化日志（分级/时间戳/模块名/可选写文件）、采集数据质量治理（无效数据打标不预警）、采集日志支持按状态过滤（ok/skip/invalid/error）
- **Sprint 3**：视频流在线状态动态探测（每 60 秒，自动判定可达/不可达，手动下线优先）、GET /api/streams/health、前端实时探测状态展示
- **Sprint 4**：接入 ZLMediaKit 流媒体服务器（server/zlm.js）、RTSP/GB28181 经 ZLM 转 FLV/WebRTC、ffmpeg 降级容错、ZLM 配置与测试接口、前端 RTSP 自动转换播放、Docker 部署指南

---

## 九、总结

系统已从纯前端原型演进为**前后端贯通、数据可靠、采集与预警闭环**的可用系统。视频监控与大气采集预警两条主线均已打通真实数据。后续重点为：接入专业流媒体服务器补齐 RTSP/GB28181 实时取流、落地短信推送、增加接口鉴权，推进至生产可用。
