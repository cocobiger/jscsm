# 端口映射申请单 — JSC 驾驶舱系统

| 字段 | 内容 |
|------|------|
| 申请人 | （你的名字） |
| 申请时间 | 2026-06-16 |
| 业务名称 | 江津大屏驾驶舱系统（JSC） |
| 内网服务器 | 172.16.8.12（Ubuntu 22.04） |
| 公网入口 | 111.10.220.226 |
| 紧急程度 | 中 |
| 期望完成时间 | 2026-06-17 前 |

---

## 一、背景

为配合江津大屏驾驶舱（JSC）系统上线，需要在网关 172.16.8.1 上增加端口映射，使外网用户能访问 JSC 系统。

服务器上已有「大气快检」项目（/admin/、/dajiang/）在 80 端口对外提供前端服务，但目前**外网实际也无法访问**（http://111.10.220.226/admin/ 返回 502）。本次申请同时修复大气快检的外网访问。

## 二、参考：现有映射

| 公网 | 内网 | 协议 | 用途 |
|------|------|------|------|
| 22 / 22333 | 22 / 22333 | TCP | SSH |

> 经核查，目前外网只能访问 SSH，其他端口（包括 80）都未做 DNAT 映射。

## 三、申请映射清单

### 推荐方案 A：最小化（够用即可）

| 公网端口 | 内网 IP | 内网端口 | 协议 | 用途 | 必要性 |
|----------|---------|----------|------|------|--------|
| **80** | 172.16.8.12 | 80 | TCP | nginx 统一入口：大屏驾驶舱(/jsc/)、大气快检(/admin/, /dajiang/) | **必须** |
| **5540** | 172.16.8.12 | 5540 | TCP | ZLMediaKit RTSP（监控摄像头/NVR 推流入口） | **必须**（推流） |
| **1936** | 172.16.8.12 | 1936 | TCP | ZLMediaKit RTMP（备用推流协议） | 可选 |
| **30000-30050** | 172.16.8.12 | 30000-30050 | UDP | ZLMediaKit RTP 媒体流回放 | **必须**（拉流播放） |

### 备选方案 B：完整（如果要走 HTTPS / WebRTC）

在方案 A 基础上额外增加：

| 公网端口 | 内网 IP | 内网端口 | 协议 | 用途 |
|----------|---------|----------|------|------|
| 443 | 172.16.8.12 | 443 | TCP | HTTPS 入口（如果后续上证书） |
| 4443 | 172.16.8.12 | 4443 | TCP | ZLMediaKit HTTPS（WebRTC over HTTPS） |
| 8080-8089 | 172.16.8.12 | 8080-8089 | UDP | ZLMediaKit WebRTC 媒体流 |

## 四、关键服务对照

| 服务 | 监听地址 | 端口 | 协议 | 状态 |
|------|----------|------|------|------|
| nginx | 0.0.0.0:80 | 80 | TCP | ✅ 运行中 |
| jsc-backend | 0.0.0.0:7170 | 7170 | TCP | ✅ 运行中（systemd） |
| ZLMediaKit HTTP | 0.0.0.0:6080 | 6080 | TCP | ✅ 容器运行 |
| ZLMediaKit RTSP | 0.0.0.0:5540 | 5540 | TCP | ✅ 容器运行 |
| ZLMediaKit RTMP | 0.0.0.0:1936 | 1936 | TCP | ✅ 容器运行 |
| ZLMediaKit HTTPS | 0.0.0.0:4443 | 4443 | TCP | ✅ 容器运行 |
| ZLMediaKit RTP | 0.0.0.0:30000-30500 | 30000-30500 | UDP | ✅ 容器运行 |
| ZLMediaKit WebRTC | 0.0.0.0:8080-8089 | 8080-8089 | UDP | ✅ 容器运行 |

## 五、推荐放行策略（iptables / 安全组）

1. **80 端口**：建议限制源 IP（仅放行办公网段/合作方 IP），或全网开放（看安全等级要求）
2. **5540 / 1936**：建议限制源 IP（推流设备 IP 白名单）
3. **30000-30050 UDP**：建议限制源 IP（拉流客户端 IP 白名单）

> 如果无法做源 IP 限制，全网开放也可（生产环境建议加 IP 白名单）。

## 六、映射后验证方法

映射完成后，在外网机器上执行：

```bash
# 验证 80
curl -I http://111.10.220.226/jsc/        # 应返回 200
curl -I http://111.10.220.226/admin/       # 应返回 200（大屏快检）
curl -I http://111.10.220.226/dajiang/     # 应返回 200（大屏快检）

# 验证 RTSP
nc -vz 111.10.220.226 5540

# 验证 RTP UDP
nc -uvz 111.10.220.226 30000
```

## 七、负责人 / 联系方式

| 角色 | 姓名 | 联系方式 |
|------|------|----------|
| 申请人 | （你的名字） | （你的电话/微信） |
| 服务器管理员 | （你或同事） | （联系方式） |
| 网关管理员 | （待对接） | （待填） |

## 八、附注

- 大气快检项目已使用 `/admin/` 和 `/dajiang/` 路径，**不会与 JSC 的 `/jsc/` 路径冲突**
- JSC 和大气快检共用同一个 80 端口，由 nginx 内部按路径分发
- 服务器自身无防火墙限制（ufw inactive，iptables ACCEPT），所有端口都已监听
- 如果映射出现问题，联系我进行联调测试

---

**附：nginx 当前路由一览**

```
/admin/      → /var/www/admin/         （大气快检后台）
/dajiang/    → /var/www/dajiang/       （大气快检大屏）
/jsc/        → /opt/jsc/frontend/      （JSC 前端）
/jsc/api/    → http://127.0.0.1:7170   （JSC 后端）
/api/        → http://127.0.0.1:7170   （兼容大气快检的 API 路径）
```
