# 代码迭代方案

> 生成日期：2026-06-08
> 依据：《系统功能说明与完善建议.md》第七章完善建议
> 主线：**先补生产可用性**（鉴权 → 错误处理/日志 → 数据质量 → 流媒体 → 短信）

---

## 总体路线

按"低风险见效快 → 核心短板 → 业务闭环"排序，分 5 个迭代。每个迭代独立可交付、可验证。

| 迭代 | 主题 | 优先级 | 预估工作量 | 外部依赖 |
|------|------|--------|-----------|----------|
| Sprint 1 | 接口鉴权 + 统一错误处理 | 高 | 中 | 无 |
| Sprint 2 | 结构化日志 + 数据质量治理 | 高 | 小 | 无 |
| Sprint 3 | 设备在线状态动态检测 | 中 | 中 | 无 |
| Sprint 4 | 流媒体服务器接入（RTSP/GB28181 取流） | 高 | 大 | ZLMediaKit |
| Sprint 5 | 短信预警推送闭环 | 中 | 大 | 云 MAS 账号 |

---

## Sprint 1：接口鉴权 + 统一错误处理

**目标**：后端所有写接口加鉴权，杜绝裸奔；前后端统一错误响应格式与提示。

**后端改动**
- 新增 `server/auth.js`：简单 Token 中间件（启动生成或从 `data/config.json` 读 API Key）
- `index.js`：对所有 POST/PATCH/DELETE 接口挂载鉴权中间件；GET 只读接口可放行或同样校验
- 统一错误响应：封装 `sendError(res, code, msg)`，所有接口返回 `{ ok:false, error, code }` 一致结构
- 新增 `GET /api/auth/check` 用于前端校验 Token 有效性

**前端改动**
- `DashboardContext` 及各 admin 页：fetch 统一带 `Authorization` 头
- 封装 `apiFetch()` 工具：自动加 Token、统一解析错误、401 时提示重新登录
- 管理后台增加一个简单的 Token 输入/保存入口（localStorage 存）

**涉及文件**：`server/index.js`、`server/auth.js`(新)、`src/app/context/DashboardContext.tsx`、`src/app/lib/apiFetch.ts`(新)、各 admin 页

**验收标准**
- 不带 Token 调用写接口返回 401
- 前端带 Token 后所有增删改正常
- 错误提示能显示后端返回的具体原因

---

## Sprint 2：结构化日志 + 数据质量治理

**目标**：后端日志可追溯；采集数据入库即剔除无效值。

**后端改动**
- 引入 pino（纯 JS，无编译）：替换散落的 console.log，分级别（info/warn/error）输出，可选写文件 `data/logs/`
- 采集入库前增强校验：AQI=0 或全部污染物为空的记录标记为"无效"，不参与预警历史窗口（可入库但打标，或直接跳过并记日志）
- `/api/collect-logs` 增加 invalid 计数

**前端改动**
- 无强制改动；可在采集日志页增加"无效数据"过滤标签

**涉及文件**：`server/index.js`、`server/logger.js`(新)、`server/crawler.js`

**验收标准**
- 后端日志带时间戳、级别、模块
- AQI=0 的记录不再污染预警判断
- 采集日志能区分"入库/跳过/无效/错误"

---

## Sprint 3：设备在线状态动态检测

**目标**：视频流的 offline 字段从手动标记升级为自动探测。

**后端改动**
- 新增探测调度器：定期（如每 60 秒）对启用的视频流做轻量连通性检测
  - HTTP-FLV/HLS：HEAD/GET 探测地址可达性
  - RTSP：可选 ffprobe 探测（需 ffmpeg）
- 探测结果写回 stream 的 `offline` 字段 + `lastCheckedAt`
- 新增 `GET /api/streams/health` 返回各流实时探测状态

**前端改动**
- 视频流管理页展示"最后检测时间"与自动在线状态
- 大屏设备在线率改为基于真实探测数据

**涉及文件**：`server/index.js`、`server/stream-monitor.js`(新)、`src/app/components/admin/VideoStreamPage.tsx`、`src/app/components/LeftPanel.tsx`

**验收标准**
- 拔掉某摄像头地址后，1 分钟内前端显示离线
- 不影响手动上下线（手动优先级可配）

---

## Sprint 4：流媒体服务器接入（核心短板）

**目标**：打通 RTSP 与 GB28181 的真实取流，让非 FLV/WebRTC 的摄像头也能播放。

**前置**：本机/服务器部署 ZLMediaKit（推荐，一体化支持 RTSP/RTMP/GB28181/WebRTC/FLV）

**后端改动**
- `server/index.js` 的 `/api/stream/start` 重写：不再裸调 ffmpeg 推 rtmp，而是调用 ZLMediaKit 的 REST API（addStreamProxy）将 RTSP 拉流并输出 FLV/WebRTC，返回 ZLMediaKit 的播放地址
- GB28181：对接 ZLMediaKit 或 WVP-PRO 的国标信令，`/api/gb28181/invite` 返回真实可播地址
- 新增 `server/zlm.js`：封装 ZLMediaKit API 调用（addStreamProxy/delStreamProxy/getMediaList）
- 数据源/视频流配置增加 ZLMediaKit 服务地址配置项

**前端改动**
- VideoPlayerModal：RTSP/GB28181 流改为请求后端拿到转换后的 FLV/WebRTC 地址再播放
- 视频流管理页：RTSP 类型流增加"启动转发"按钮与转发状态

**涉及文件**：`server/index.js`、`server/zlm.js`(新)、`src/app/components/VideoPlayerModal.tsx`、`src/app/components/admin/VideoStreamPage.tsx`

**验收标准**
- 配一条真实 RTSP 摄像头，点播放能出画面
- GB28181 设备能通过国标编码取流播放

---

## Sprint 5：短信预警推送闭环

**目标**：预警命中后自动发短信给责任人，落地《开发文档》第 3、4 章。

**后端改动**
- 新增数据文件：`contacts.json`(联系人)、`sms_config.json`(云MAS配置)、`sms_history.json`(推送历史)、`sms_templates.json`(模版)
- 新增模块 `server/sms.js`：
  - MAC 校验码生成（MD5，按文档拼接顺序）
  - 普通/模板短信下发（form-urlencoded + BASE64）
  - 黑白名单过滤、批次拆分、失败重试
  - 占位符替换（时间/点位/污染物/预警类型）
- 预警引擎命中后触发：按点位匹配联系人 → 套模版 → 调 sms.js 下发 → 记录推送历史
- 状态报告回调接口 `POST /api/sms/report`
- 配置驱动：默认不真实发送，填入真实云 MAS 参数后启用

**前端改动**
- 气体采集预警模块新增子页：联系人管理、短信接口配置、模版管理、推送历史、黑白名单

**涉及文件**：`server/index.js`、`server/sms.js`(新)、`src/app/components/admin/`(新增页面)

**验收标准**
- 模拟一条超标数据 → 预警命中 → 推送历史出现一条记录（占位模式不真实发）
- 填真实云 MAS 参数后能收到短信
- MAC 校验、模版替换、黑名单过滤正确

---

## 横向贯穿（每个迭代都遵守）

- 每个迭代完成后更新《系统功能说明与完善建议.md》对应章节
- 后端改动后 `node --check` 全部文件，关键逻辑写隔离测试
- 前端改动后括号平衡检查 + 手动验证
- 数据文件结构变更时，启动自愈与备份机制同步覆盖新文件

---

## 低优先级（择机插入）

- 高德地图 Key 迁移到环境变量
- MQTT 真实 Broker 接入
- 采集数据趋势分析 / 超标统计报表

---

## 实施顺序建议

```
Sprint 1（鉴权+错误处理）── 立即开始
        ↓
Sprint 2（日志+数据质量）── 小而快，紧接其后
        ↓
Sprint 3（设备探测）─────── 独立，可并行
        ↓
Sprint 4（流媒体）───────── 需先部署 ZLMediaKit
        ↓
Sprint 5（短信）─────────── 需云 MAS 账号
```
