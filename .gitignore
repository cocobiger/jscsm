# 依赖
node_modules/
server/node_modules/

# 构建产物
dist/
build/

# 环境变量（含密钥，不提交）
.env
.env.local
.env.*.local
iotcloud.env
*.env

# 运行时数据 / 数据库 / 密钥载体（不提交）
server/data/
backups/
*.rar
*.zip

# 临时 / 缓存 / 垃圾文件
*.log
*.tmp
*.tmp.*
tmp_*
__pycache__/
*.pyc
nul
ziOHU5IN
*.reg

# 编辑器/系统
.DS_Store
.vscode/
.idea/

# 本地工作记忆与生成物（非源码）
.workbuddy/
outputs/

# 备份快照与归档
*.bak
*.bak.*

# Python 虚拟环境
.venv/
venv/

# 散落的 ZLMediaKit 配置（权威版在 infra/zlm/）
/zlm_config.ini

# 运行时数据 JSON（非源码，易变且 streams.json 含密钥）
server/collect_logs.json
server/collected.json
server/streams.json
server/warnings.json
server/datasources.json
server/map_points.json
server/*.bak*
server/*.bak[0-9]

# 杂项截图
*.workbuddyclipboard*
